<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="seatMapper">
	<select id="bookCheck" resultType="SeatVO" parameterType="ChoiceVO">
		select seat_name from seat where cinema_num = #{cinema_num} 
	</select>
	
</mapper>